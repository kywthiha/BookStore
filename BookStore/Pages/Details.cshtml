@page
@model BookStore.Pages.DetailsModel

@{
    ViewData["Title"] = "Details";
    ViewData["counts"] = Model.BookCatsInfo;
}

<h1>Details</h1>

<div>
    <h4>Book</h4>
    <hr />
    <div class="row">
        <div class="col-md-3">
            <img id="blah" src="~/images/@Model.Book.Cover" alt="your image" class="rounded img-thumbnail" />
        </div>
        <div class="col-md-9">
            <dl class="row">
                <dt class="col-sm-2">
                    @Html.DisplayNameFor(model => model.Book.Title)
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.Book.Title)
                </dd>
                <dt class="col-sm-2">
                    @Html.DisplayNameFor(model => model.Book.Author.Name)
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.Book.Author.Name)
                </dd>
                <dt class="col-sm-2">
                    @Html.DisplayNameFor(model => model.Book.Price)
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.Book.Price)
                </dd>
                <dt class="col-sm-2">
                    @Html.DisplayNameFor(model => model.Book.Category.Title)
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.Book.Category.Title)
                </dd>
                <dt class="col-sm-2">
                    @Html.DisplayNameFor(model => model.Book.Summary)
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.Book.Summary)
                </dd>
            </dl>
            <a asp-page="./Details" asp-route-id="@Model.Book.ID" asp-route-addCatId="@Model.Book.ID" class="btn btn-primary btn-sm">Add To Card <i class="fa fa-cart-plus" aria-hidden="true"></i></a>

        </div>
        </div>
    </div>
    
<div>
    <a asp-page="./Index">Back to List</a>
</div>
